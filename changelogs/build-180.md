Polishes the Today tooltip and Companion chat-bubble pointers so the bubble and arrow render as one continuous stroked shape.

Tightens the Today bridge arrow spacing so it points at the Companion ring without crowding it.

Adds regression coverage for shared bubble-tail geometry across tooltip and chat surfaces.
