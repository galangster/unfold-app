import { PastSeriesLibraryScreen } from '../(you)/past-devotionals';

/** Devotional › Past series. Opens each series' arc inside the Study stack. */
export default function StudyPastDevotionalsScreen() {
  return <PastSeriesLibraryScreen hostTab="(study)" />;
}
