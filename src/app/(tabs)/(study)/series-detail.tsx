import { SeriesArcScreen } from '../(you)/series-detail';

/** A past series' arc, opened from the Study library and kept in Study. */
export default function StudySeriesDetailScreen() {
  return <SeriesArcScreen hostTab="(study)" />;
}
