import JournalScreen from '../(today)/journal';

/** Reader journal composer hosted in the Study stack. */
export default function StudyJournalScreen() {
  return <JournalScreen hostTab="(study)" />;
}
