import MyContentScreen from '../(you)/my-content';

/** Saved highlights / bookmarks opened from the Study reader. */
export default function StudyMyContentScreen() {
  return <MyContentScreen hostTab="(study)" />;
}
