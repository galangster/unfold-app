import { ReadingScreen } from '../(today)/reading';

/** Devotional reader hosted in the Study stack. */
export default function StudyReadingScreen() {
  return <ReadingScreen hostTab="(study)" />;
}
