import { DayMenuScreen } from '../(today)/day-menu';

/** Day picker sheet hosted in the Study stack. */
export default function StudyDayMenuScreen() {
  return <DayMenuScreen hostTab="(study)" />;
}
