import { SeriesArcScreen } from '../(you)/series-detail';

/** Study tab root — Book of Seasons for the current series. */
export default function StudyIndexScreen() {
  return <SeriesArcScreen hostTab="(study)" chrome="tabRoot" />;
}
